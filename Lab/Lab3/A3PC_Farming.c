#include <stdio.h>
#include <math.h>

int max_minSpace(long int availLoc[], long int start, long int end)
{
    // probably a recursive solution ;|
}

int main(int argc, char const *argv[])
{
    long int N, M;
    scanf("%d%d", &N, &M);
    long int availLoc[M];
    for (int i = 0; i < M; i++)
    {
        scanf("%ld", &availLoc[i]);
    }
    

    return 0;
}
